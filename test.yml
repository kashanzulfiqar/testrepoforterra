name: "Workflow 1 - Deployment Test"

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  test-job-one:
    runs-on: ubuntu-latest
    steps:
      - name: Print Start Message
        run: echo "🚀 Starting Workflow 1: Simulating deployment steps..."

      - name: Print Status Output
        run: |
          echo "Checking environment..."
          echo "Running build scripts..."
          echo "✅ Workflow 1 completed successfully!"
